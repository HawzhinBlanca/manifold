; MANIFOLD — engine configuration (PC / Windows-first, deterministic simulation).
;
; The auto-generated AndroidFileServer settings block (and its local dev
; SecurityToken) was removed: this project targets PC and does not use Android
; file serving. If the AndroidFileServer plugin is ever enabled, the editor will
; regenerate that section here — disable the plugin to keep it out.

[/Script/WindowsTargetPlatform.WindowsTargetSettings]
DefaultGraphicsRHI=DefaultGraphicsRHI_DX12

[/Script/Engine.Engine]
; Deterministic fixed-step simulation is owned by the kernels; keep engine-side
; frame-rate smoothing out of the way so headless runs stay reproducible.
bSmoothFrameRate=False
